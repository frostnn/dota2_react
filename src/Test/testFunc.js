/* const [idPlayers, setIdPlayers] = useState([]); */

/* const getIdPlayers = () => {
    const newTeamsPlayers = [...teamsPlayers];
    const id = [];
    newTeamsPlayers
      .filter((players) => {
        if (players.name && players.is_current_team_member) {
          return players;
        }
      })
      .map((players) => {
        id.push(players.account_id);
      });
    return setIdPlayers(id);
  }; */
